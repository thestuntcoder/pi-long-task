{
  "name": "pi-coordinator",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Native Pi TODO coordinator extension package.",
  "keywords": ["pi-package"],
  "scripts": {
    "test": "node --experimental-strip-types --test"
  },
  "pi": {
    "extensions": ["./src/index.ts"]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-ai": "*",
    "typebox": "*"
  }
}
